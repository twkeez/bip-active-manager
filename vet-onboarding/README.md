# Deprecated standalone scaffold

This folder was created as a separate Next.js app by mistake. The vet onboarding module now lives in the main BIP Control Panel:

- **Route:** `/vet-onboarding`
- **Component:** `components/vet-onboarding/`
- **API:** `app/api/vet-onboarding/generate/route.ts`

Run the main app from the repo root (`npm run dev`), not from this folder.

You can delete this `vet-onboarding/` directory when ready.
